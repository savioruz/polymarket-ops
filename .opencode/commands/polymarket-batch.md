# /polymarket-batch — Batch analyze all watched wallets

Load modes/batch.md and config/watchlist.yml.

Analyze all active wallets in parallel.
Produce a unified signal table with all actionable copy signals.
Flag positions that need attention (whale exited, high probability, stop loss).

Output to reports/batch-signals-{DATE}.md
