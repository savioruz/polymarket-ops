# /polymarket-portfolio — View your portfolio and P&L

Load modes/portofolio.md and config/profile.yml.

Fetch your wallet's current positions, closed positions, and trade history.
Cross-reference with data/tracker.tsv (local, gitignored) to show copy trade metadata.
Generate a full dashboard report.

Update data/positions.json with the current snapshot.
