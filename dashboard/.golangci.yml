version: "2"

run:
  timeout: 3m

linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - ineffassign
    - unused
    - errorlint

formatters:
  enable:
    - gofmt

issues:
  exclude-use-default: false
